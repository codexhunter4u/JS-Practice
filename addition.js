const num1 = 5;
const num2 = 3;

// add two numbers
const sum = num1 + num2;

// Add this line to each file to see the output
var output = 'The sum of ' + num1 + ' and ' + num2 + ' is: ' + sum;
$('#output').text(output);
